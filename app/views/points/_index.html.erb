<h1>Points#index</h1>
<p>Find me in app/views/points/index.html.erb</p>

<div class="#">
    <% @points.each do |point|  %>
    <%= point.title %>
    <%= point.text %>
    <%= image_tag point.image, class: "#", :size => "260x180" if point.image.attached?  %>
    <br>
    <% end %>

</div>

<%= link_to "アピールポイントを作成する", new_product_point_path(@product.id), class:"#" %>
